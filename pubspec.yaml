name: acervoitaboraiense
description: Aplicativo do Acervo Itaboraiense
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  url_launcher: ^6.3.2
  material_ui: ^1.0.0
  cupertino_ui: ^1.0.0

flutter:
  uses-material-design: true
