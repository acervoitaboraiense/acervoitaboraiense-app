name: Build APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Java 17
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Configurar Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.0'
          channel: 'stable'
          cache: true

      - name: Gerar estrutura Android (temporário)
        run: |
          flutter create --platforms=android \
            --project-name acervo_itaborai \
            --org br.shop.memoriaitaboraiense \
            /tmp/scaffold
          cp -r /tmp/scaffold/android .
          sed -i '/<application/i \    <uses-permission android:name="android.permission.INTERNET" \/>' android/app/src/main/AndroidManifest.xml

      - name: Instalar dependências
        run: flutter pub get

      - name: Build do APK (release)
        run: flutter build apk --release

      - name: Renomear APK
        run: |
          mv build/app/outputs/flutter-apk/app-release.apk \
             build/app/outputs/flutter-apk/acervo-itaborai-${{ github.ref_name }}.apk

      - name: Salvar APK como artifact
        uses: actions/upload-artifact@v4
        with:
          name: acervo-itaborai-apk
          path: build/app/outputs/flutter-apk/*.apk

      - name: Criar Release e anexar APK
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/app/outputs/flutter-apk/*.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
